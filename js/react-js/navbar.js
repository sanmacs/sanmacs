import ReactDOM from 'react-dom';

function MyApp() {
    return (
        <div>
            <h1>Hello, I'm a React App!</h1>
        </div>
    );
}

ReactDOM.render(<MyApp />, document.getElementById('react-app-container'));
